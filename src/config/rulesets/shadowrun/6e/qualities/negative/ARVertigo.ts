import { Quality } from "../Quality";

export class ARVertigo implements Quality {
    name = "AR Vertigo";
    cost = -10;
    effect = "Cannot gain or spend edge in AR. Gain Nauseated for 1 hour after AR use.";
}